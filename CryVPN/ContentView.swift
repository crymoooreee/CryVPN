import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("CryVPN")
                    .font(.largeTitle)
                Text("Demo Project Structure")
            }
            .padding()
        }
    }
}
